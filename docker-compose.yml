services:
    stardew:
        image: ghcr.io/samuel-lgd/stardew-valley-server:latest
        container_name: sv-server
        ports:
            - ${GAME_PORT}:24642/udp
            - ${VNC_PORT}:5800
        volumes:
            - game:/data/Stardew:z
            - data:/config/xdg/config/StardewValley:z
        environment:
            STEAM_USER: "${STEAM_USER}"
            STEAM_PASS: "${STEAM_PASS}"
            STEAM_GUARD_CODE: "${STEAM_GUARD_CODE}"
            VNC_PASSWORD: "${VNC_PASSWORD}"
            DISABLE_RENDERING: "${DISABLE_RENDERING}"

volumes:
  game:
  data:
